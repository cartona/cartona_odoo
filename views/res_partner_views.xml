<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_partner_form_cartona" model="ir.ui.view">
        <field name="name">res.partner.form.cartona</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="cartona_id" readonly="1" invisible="not is_cartona_customer"/>
                <field name="is_cartona_customer" invisible="1"/>
            </xpath>
        </field>
    </record>

    <record id="view_res_partner_filter_cartona" model="ir.ui.view">
        <field name="name">res.partner.search.cartona</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='inactive']" position="after">
                <filter name="filter_cartona_customers" string="Cartona Customers"
                        domain="[('is_cartona_customer', '=', True)]"/>
            </xpath>
        </field>
    </record>

</odoo>
